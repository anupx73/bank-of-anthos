apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: unknown-balance
  namespace: boa
spec:
  hosts:
    - balancereader.boa.svc.cluster.local
  http:
    - directResponse:
        body:
          string: unknown error
        status: 503